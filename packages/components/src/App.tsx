import { Button } from "app";
import React from "react";

interface Props {}

const App: React.FC<Props> = (props) => {
  // props and metadata

  // states

  // configs

  // constants

  // actions

  // renderers
  return (
    <div>
      <title>Hello</title>
      <Button />
    </div>
  );
};

export default App;
